# Hardware report (experimental)

`dr_down` analyzes the R markdown capabilities of the hardware.

## Usage

``` r
dr_down()
```

## Examples

``` r
if (FALSE) { # \dontrun{
  indiedown::dr_down()
} # }
```
