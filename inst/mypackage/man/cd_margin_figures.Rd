% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cd_margin_figures.R
\name{cd_margin_figures}
\alias{cd_margin_figures}
\title{Enable tufte-style margin and full-width figures}
\usage{
cd_margin_figures(format, width = 45)
}
\arguments{
\item{format}{A \code{rmarkdown} output format, typically the result of the
package's own format wrapper.}

\item{width}{Console width passed to \code{knitr} (\code{opts_knit$width}); narrower
values wrap printed output to fit margin notes. Defaults to \code{45}.}
}
\value{
The modified \code{format}, invisibly friendly for piping.
}
\description{
Optional module. Call it on the format returned by
\code{indiedown_pdf_document_with_asset()} to map two chunk options onto
dedicated float environments:
}
\details{
\itemize{
\item \code{fig.margin = TRUE} places the figure in the margin (\code{marginfigure}),
\item \code{fig.fullwidth = TRUE} spans the full text width (\verb{figure*}).
}

It also registers a \code{marginfigure} knitr engine so that \verb{```\{marginfigure\}}
blocks (margin text, not figures) are wrapped in the same environment.

The LaTeX side needs the \code{marginfigure} environment. The skeleton
\code{preamble.tex} ships a commented-out \verb{\\\\usepackage\{sidenotes\}} line that
provides it (and makes \verb{figure*} behave); uncomment it when you opt in.
}
\examples{
\dontrun{
mypackage <- function(...) {
  format <- indiedown_pdf_document_with_asset(...)
  cd_margin_figures(format)
}
}
}
